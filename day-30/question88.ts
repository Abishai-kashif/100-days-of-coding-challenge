// Rounding to the Nearest Integer:     -Question 88

// this function takes a decimal input & rounds it to the nearest integer
function roundOf(decimal: number): number {
	return Math.round(decimal);
}

// calling the func & loging the rounded integer
console.log(roundOf(3.4)); // output: 3
