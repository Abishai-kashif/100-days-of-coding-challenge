"use strict";
// demonstrate using console.log() inside loop to track variable values
for (let i = 1; i <= 5; i++) {
    console.log(`Iteration ${i}, value of i: ${i}`); // tracking variable values on each iteration
}
// This loop iterates five times, logging the value of 'i' during each iteration to help with debugging.
