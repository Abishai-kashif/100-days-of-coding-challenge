console.log("Before Breakpoint");

console.log(
	"this line has the breakpoint set on it in the browser's dev tools"
);

console.log("After Breakpoint");
/*  to use breakpoints, open this code in your browser,
    open browser's dev tools,
    navigate to source tab,
    find this script
    and click on the line where you want to pause execution.
    When you run or reload(ctrl+R) the page,
    execution will pause at the breakpoint.
*/
