var countDown = 10;
// the while loop is counting down from 10 to 1
while (countDown > 0) {
    if (countDown === 5)
        break; // stops the loop when countDown = 5
    console.log(countDown); // so, countdown 10-6
    countDown--; // decrementing countDown
}
